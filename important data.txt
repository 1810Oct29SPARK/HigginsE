armada games
8:30 PM friday
booster draft
13 dollars
scriptable object unity

for (String a : arrayOfShortenedString) 
            System.out.println(a);
        	acronymArray[a]=a[1];

(done) Without using the StringBuilder or StringBuffer class, write a method that reverses a String.
(done) Convert a phrase to its acronym.
Determine if a triangle is equilateral, isosceles, or scalene.
Given a word, compute the scrabble score for that word.
Clean up user-entered phone numbers so that they can be sent SMS messages.
Given a phrase, count the occurrences of each word in that phrase.
Implement a binary search algorithm.
Implement a program that translates from English to Pig Latin.
(done)An Armstrong number is a number that is the sum of its own digits each raised to the power of the number of digits.
Compute the prime factors of a given natural number.
Create an implementation of the rotational cipher, also sometimes called the Caesar cipher.
(done)Given a number n, determine what the nth prime is.
Create an implementation of the atbash cipher, an ancient encryption system created in the Middle East. Encode a value with the cipher
Create an implementation of the atbash cipher, an ancient encryption system created in the Middle East. Decode a value with the cipher
Write a method that validates a number to be proper ISBN-10.
Determine if a sentence is a pangram.
Calculate the moment when someone has lived for 10^9 seconds.
(done)Given a number, find the sum of all the unique multiples of particular numbers up to but not including that number.
Given a number determine whether or not it is valid per the Luhn formula.
Parse and evaluate simple math word problems returning the answer as an integer.

file>import>maven
existing maven projects
(choose folder one above workspace)

new branch off of Master with name1name2,
make changes there

git pull origin master on my branch in exercise submissions


("046 454 286");
if (text.matches("[0-9]+") && text.length() > 2) {